version: 1

frontend:
  phases:
    preBuild:
      commands:
        - cd backend/client
        - npm install
    build:
      commands:
        - npm run build
        - npm install -g serve
        - serve -s build