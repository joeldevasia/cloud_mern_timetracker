version: 1

backend:
  phases:
    preBuild:
      commands:
        - cd backend
        - npm ci 
        
    build:
      commands:
        - npm start
    
frontend:
  phases:
    preBuild:
      commands:
        - cd backend/client
        - npm install 
        
    build:
      commands:
        - npm start